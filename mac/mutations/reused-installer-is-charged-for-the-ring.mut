# The bar starts half-way along for a download that never happened.
filter: WriteBase
file: Sources/BootItKit/RunPlan.swift
--- old ---
        case .macos:   return installerWasReused ? 0 : 0.5
--- new ---
        case .macos:   return 0.5
